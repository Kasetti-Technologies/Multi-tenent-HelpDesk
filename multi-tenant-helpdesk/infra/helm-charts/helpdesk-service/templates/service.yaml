apiVersion: v1
kind: Service
metadata:
  name: {{ include "helpdesk-service.fullname" . }}
  labels:
    {{- include "helpdesk-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "helpdesk-service.selectorLabels" . | nindent 4 }}