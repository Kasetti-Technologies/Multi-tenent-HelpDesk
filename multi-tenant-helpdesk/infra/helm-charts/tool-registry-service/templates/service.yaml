apiVersion: v1
kind: Service
metadata:
  name: {{ include "tool-registry-service.fullname" . }}
  labels:
    {{- include "tool-registry-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tool-registry-service.selectorLabels" . | nindent 4 }}